package org.example.Codewars.scramblies;

import java.util.stream.Stream;

public class Scrambler {
    public boolean scramble(String str1, String str2) {
        char[] charArray = str2.toCharArray();
        return true;
    }
}
